/**
 * tests/recuperar-password/recuperar.test.js
 *
 * Archivo reservado para pruebas del flujo de recuperación de contraseña
 * (pasos UI + validaciones + envío de payload a la API).
 */

test.todo('POST /api/recuperar/solicitar — envía OTP al email');
test.todo('POST /api/recuperar/verificar — valida código OTP');
test.todo('POST /api/recuperar/nueva-password — actualiza contraseña');

